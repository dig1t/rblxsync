name: Release

# Bumping the version in the VERSION file and pushing it to main cuts a release:
# the version is synced into Cargo.toml/Cargo.lock, committed, tagged `v<version>`,
# and prebuilt binaries for Linux, macOS, and Windows are attached to the release.
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['VERSION']

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      sha: ${{ steps.commit.outputs.sha }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Read version
        id: version
        run: echo "version=$(tr -d '[:space:]' < VERSION)" >> "$GITHUB_OUTPUT"

      - name: Sync version into Cargo.toml and Cargo.lock
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          # First `version = "..."` line is the [package] version.
          perl -0pi -e 's/^version = "[^"]*"/version = "$ENV{VERSION}"/m' Cargo.toml
          # The version line directly after the rblxsync lockfile entry.
          perl -0pi -e 's/(^name = "rblxsync"\n)version = "[^"]*"/$1version = "$ENV{VERSION}"/m' Cargo.lock

      - name: Commit version bump
        id: commit
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet; then
            echo "Cargo.toml/Cargo.lock already at v$VERSION"
          else
            git add Cargo.toml Cargo.lock
            git commit -m "Release v$VERSION"
            git push origin main
          fi
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  build:
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            os: macos-latest  # Intel binary cross-compiled on Apple Silicon (Intel macos-13 runners retired)
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.prepare.outputs.sha }}

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8  # stable branch, pinned 2026-06-12
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --locked --target ${{ matrix.target }}

      # Aftman installs tools from .zip archives, not bare binaries. Each archive
      # is named per-target; the executable INSIDE is `rblxsync` (`rblxsync.exe`
      # on Windows) so Aftman resolves it consistently.
      - name: Stage binary (unix)
        if: runner.os != 'Windows'
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          TARGET: ${{ matrix.target }}
        run: |
          cp "target/$TARGET/release/rblxsync" rblxsync
          zip "rblxsync-v$VERSION-$TARGET.zip" rblxsync
          echo "ASSET=rblxsync-v$VERSION-$TARGET.zip" >> "$GITHUB_ENV"

      - name: Stage binary (windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          VERSION: ${{ needs.prepare.outputs.version }}
          TARGET: ${{ matrix.target }}
        run: |
          Compress-Archive -Path "target/$env:TARGET/release/rblxsync.exe" -DestinationPath "rblxsync-v$env:VERSION-$env:TARGET.zip"
          "ASSET=rblxsync-v$env:VERSION-$env:TARGET.zip" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: rblxsync-${{ matrix.target }}
          path: ${{ env.ASSET }}

  release:
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Publish release
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b  # v3.0.1
        with:
          tag_name: v${{ needs.prepare.outputs.version }}
          name: v${{ needs.prepare.outputs.version }}
          target_commitish: ${{ needs.prepare.outputs.sha }}
          generate_release_notes: true
          files: dist/*
